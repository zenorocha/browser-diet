<!doctype html>
<html lang="pt-BR">
<head>

  <meta charset="utf-8">
  <title><%= @getTitle() %></title>

  <!-- Style -->
  <link rel="stylesheet" href="<%= @site.assets %>/css/all.min.css">

  <!-- Fonts -->
  <link rel="stylesheet" href="http://fonts.googleapis.com/css?family=Open+Sans&Pacifico&Magra">

</head>

<body class="<%= @lang %>">

  <div id="top-bar" class="subtitle">
    <h3 class="center">O guia definitivo de performance para o desenvolvedor front-end</h3>
  </div>

  <a href="https://github.com/zenorocha/como-perder-peso">
    <img id="github-ribbon" src="https://s3.amazonaws.com/github/ribbons/forkme_right_darkblue_121621.png" alt="Fork me on GitHub">
  </a>

  <div class="intro center">
    <%= @getTitleHTML() =>
    <img src="<%= @site.assets %>/img/superman.png" alt="Superman" class="superman" width="745" height="655" />
  </div>

  <div id="menu">
    <ul class="center">
      <li><a href="#html">HTML</a></li>
      <li><a href="#css">CSS</a></li>
      <li><a href="#js">JavaScript</a></li>
      <li><a href="#jquery">jQuery</a></li>
      <li><a href="#bonus">Bônus</a></li>
    </ul>
  </div>

  <div id="content" class="center">

    <%- @content %>

    <h2 id="html" class="topics">HTML</h2>

    <% for item in @getCollection("documents").findAll({ url: $startsWith: '/html/' + @getLang()}, [order:1]).toJSON(): %>
      <div class="article">
        <h1 id="<%= item.slug %>">
          <a class="tip-title" href="#<%= item.slug %>"><%= item.title %></a>
          <a title="Link" class="anchor" href="#<%= item.slug %>">∞</a>
        </h1>
        <% if item.order: %>
        <div class="line">
          <span class="order"><%= item.order %></span>
          <a title="Editar" class="edit" href="<%= item.githubURL %>">✎</a>
        </div>
        <% end %>
        <%- item.contentRenderedWithoutLayouts %>
      </div>
    <% end %>

    <h2 id="css" class="topics">CSS</h2>

    <% for item in @getCollection("documents").findAll({ url: $startsWith: '/css/' + @getLang()}, [order:1]).toJSON(): %>
      <div class="article">
        <h1 id="<%= item.slug %>">
          <a class="tip-title" href="#<%= item.slug %>"><%= item.title %></a>
          <a title="Link" class="anchor" href="#<%= item.slug %>">∞</a>
        </h1>
        <% if item.order: %>
        <div class="line">
          <span class="order"><%= item.order %></span>
          <a title="Editar" class="edit" href="<%= item.githubURL %>">✎</a>
        </div>
        <% end %>
        <%- item.contentRenderedWithoutLayouts %>
      </div>
    <% end %>

    <h2 id="js" class="topics">JavaScript</h2>

    <% for item in @getCollection("documents").findAll({ url: $startsWith: '/js/' + @getLang()}, [order:1]).toJSON(): %>
      <div class="article">
        <h1 id="<%= item.slug %>">
          <a class="tip-title" href="#<%= item.slug %>"><%= item.title %></a>
          <a title="Link" class="anchor" href="#<%= item.slug %>">∞</a>
        </h1>
        <% if item.order: %>
        <div class="line">
          <span class="order"><%= item.order %></span>
          <a title="Editar" class="edit" href="<%= item.githubURL %>">✎</a>
        </div>
        <% end %>
        <%- item.contentRenderedWithoutLayouts %>
      </div>
    <% end %>

    <h2 id="jquery" class="topics">jQuery</h2>

    <% for item in @getCollection("documents").findAll({ url: $startsWith: '/jquery/' + @getLang()}, [order:1]).toJSON(): %>
      <div class="article">
        <h1 id="<%= item.slug %>">
          <a class="tip-title" href="#<%= item.slug %>"><%= item.title %></a>
          <a title="Link" class="anchor" href="#<%= item.slug %>">∞</a>
        </h1>
        <% if item.order: %>
        <div class="line">
          <span class="order"><%= item.order %></span>
          <a title="Editar" class="edit" href="<%= item.githubURL %>">✎</a>
        </div>
        <% end %>
        <%- item.contentRenderedWithoutLayouts %>
      </div>
    <% end %>

    <h2 id="bonus" class="topics">Bônus</h2>

    <% for item in @getCollection("documents").findAll({ url: $startsWith: '/bonus/' + @getLang()}, [order:1]).toJSON(): %>
      <div class="article">
        <h1 id="<%= item.slug %>">
          <a class="tip-title" href="#<%= item.slug %>"><%= item.title %></a>
          <a title="Link" class="anchor" href="#<%= item.slug %>">∞</a>
        </h1>
        <% if item.order: %>
        <div class="line">
          <span class="order"><%= item.order %></span>
          <a title="Editar" class="edit" href="<%= item.githubURL %>">✎</a>
        </div>
        <% end %>
        <%- item.contentRenderedWithoutLayouts %>
      </div>
    <% end %>

  </div>

  <a title="Ir para o topo" id="top" href="#top-bar"><img src="<%= @site.assets %>/img/top.png" alt="Go up!"></a>

</body>
</html>